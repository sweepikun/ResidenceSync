# ResidenceSync - Minecraft 跨服务器领地同步插件

## 项目概述

ResidenceSync 是一个强大的 Minecraft 插件，支持跨服务器领地传送功能。使用最新的 Gradle 9.2 构建系统，确保高效的开发和构建体验。

## 主要特性

- ✅ **跨服务器传送**: 支持玩家在不同服务器间传送到自己的领地
- ✅ **BungeeCord 集成**: 完全支持 BungeeCord 代理服务器
- ✅ **数据库同步**: 使用 HikariCP 连接池实现高效数据库操作
- ✅ **配置缓存**: 启用 Gradle 9.2 配置缓存，显著提升构建性能
- ✅ **现代化构建**: 使用最新的 Gradle 语法和最佳实践

## 技术规格

- **目标平台**: Spigot 1.19.3+
- **Java 版本**: Java 17+
- **构建工具**: Gradle 9.2
- **数据库**: MySQL 8.0+
- **依赖管理**: Gradle 依赖缓存 + 现代化仓库配置

## 快速开始

### 环境要求

1. **Java 17+** (推荐使用 Temurin 发行版)
2. **Gradle 9.2** (推荐使用 Gradle Wrapper)

### 初始化项目

```bash
# 1. 克隆项目
git clone <repository-url>
cd ResidenceSync

# 2. 初始化 Gradle Wrapper (如果不存在)
gradle wrapper --gradle-version=9.2

# 3. 构建项目
./gradlew build

# 4. 生成插件 JAR 文件
./gradlew shadowJar
```

### 开发命令

```bash
# 运行测试
./gradlew test

# 代码质量检查
./gradlew check

# 清理构建文件
./gradlew clean

# 完整构建 + 检查
./gradlew qualityCheck
```

## 项目结构

```
ResidenceSync/
├── .github/
│   └── workflows/
│       └── ci.yml              # GitHub Actions CI/CD
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── cn/popcraft/residencesync/
│   │   └── resources/
│   └── test/
├── build.gradle                 # 主构建文件 (Gradle 9.2 语法)
├── settings.gradle              # 项目设置 (现代仓库配置)
├── gradle.properties            # Gradle 性能优化配置
└── README.md                    # 项目文档
```

## 构建优化特性

### 🎯 Gradle 9.2 优化配置

本项目使用了 Gradle 9.2 的最新特性：

1. **配置缓存**: `org.gradle.configuration-cache=true`
2. **并行构建**: `org.gradle.parallel=true`
3. **文件系统监控**: `org.gradle.vfs.watch=true`
4. **构建缓存**: `org.gradle.caching=true`
5. **现代化仓库配置**: `dependencyResolutionManagement`

### 📦 依赖管理现代化

- **专属仓库配置**: 使用 `exclusiveContent` 确保依赖解析效率
- **集中式仓库管理**: 在 `settings.gradle` 中统一管理所有仓库
- **版本锁定**: 避免依赖冲突和版本漂移

### 🏗️ GitHub Actions CI/CD

项目包含完整的 CI/CD 工作流：

- **自动化测试**: 每次推送时运行完整测试套件
- **代码质量检查**: 集成静态分析工具
- **构建缓存**: 利用 Gradle 缓存加速构建
- **工件发布**: 自动上传构建产物

## 配置说明

### 核心配置

```yaml
# plugin.yml
name: ResidenceSync
version: 1.0.0
author: MiniMax Agent
api-version: 1.19.3

main: cn.popcraft.residencesync.ResidenceSyncPlugin

depend: [Residence, PlaceholderAPI]
```

### 数据库配置

插件使用以下数据库表结构：

- `residence_sync_data`: 存储跨服务器领地信息
- `player_cache`: 缓存玩家数据以提高性能

## 部署

### 构建发布版本

```bash
# 1. 运行完整质量检查
./gradlew qualityCheck

# 2. 构建发布版本
./gradlew shadowJar

# 3. JAR 文件位置
# build/libs/ResidenceSync.jar
```

### GitHub Actions 自动部署

项目配置了完整的 CI/CD 工作流：

1. **每次推送** → 自动运行测试和构建
2. **合并到 main** → 自动构建和发布 JAR
3. **Pull Request** → 运行完整质量检查

## 性能优化

### 构建性能

- **配置缓存**: 减少 50-80% 的配置时间
- **并行执行**: 利用多核 CPU 加速构建
- **构建缓存**: 复用之前构建的结果
- **文件系统监控**: 智能检测变更

### 运行时性能

- **HikariCP 连接池**: 高效数据库连接管理
- **异步处理**: 非阻塞的跨服务器通信
- **智能缓存**: 减少数据库查询频率

## 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

### 开发环境设置

```bash
# 1. 安装 JDK 17+
# 2. 克隆项目
git clone <your-fork-url>
cd ResidenceSync

# 3. 设置开发环境
./gradlew wrapper

# 4. 运行开发构建
./gradlew build --info
```

## 许可证

本项目基于 MIT 许可证开源 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 致谢

- SpigotMC 社区提供的优秀 API
- PaperMC 团队的性能优化工作
- Gradle 团队构建系统的持续改进

## 技术支持

如遇到问题或需要支持，请：

1. 查阅 [Wiki 文档](wiki-url)
2. 搜索 [Issues](issues-url)
3. 创建新的 Issue 描述问题

---

**构建时间**: $(date)
**Gradle 版本**: 9.2
**Java 版本**: 17+